<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesource.org/schema/mule/core/2.1"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:spring="http://www.springframework.org/schema/beans"
      xmlns:context="http://www.springframework.org/schema/context"
      xmlns:pgp="http://www.mulesource.org/schema/mule/pgp/2.1"
      xmlns:xm="http://www.mulesource.org/schema/mule/xml/2.1"
      xmlns:vm="http://www.mulesource.org/schema/mule/vm/2.1"
      xmlns:stdio="http://www.mulesource.org/schema/mule/stdio/2.1"
      xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-2.5.xsd
       http://www.mulesource.org/schema/mule/core/2.1 http://www.mulesource.org/schema/mule/core/2.1/mule.xsd
       http://www.mulesource.org/schema/mule/pgp/2.1 http://www.mulesource.org/schema/mule/pgp/2.1/mule-pgp.xsd
       http://www.mulesource.org/schema/mule/xml/2.1 http://www.mulesource.org/schema/mule/xml/2.1/mule-xml.xsd
       http://www.mulesource.org/schema/mule/vm/2.1 http://www.mulesource.org/schema/mule/vm/2.1/mule-vm.xsd
       http://www.mulesource.org/schema/mule/stdio/2.1 http://www.mulesource.org/schema/mule/stdio/2.1/mule-stdio.xsd
       "
        >

    <stdio:connector name="InputSystemStreamConnector" promptMessage="Enter Some Text: "/>

    <security-manager>
        <password-encryption-strategy name="passwordEncryption" password="password"/>
    </security-manager>

    <model name="echoModel">
        <service name="in">
            <inbound>
                <stdio:inbound-endpoint connector-ref="InputSystemStreamConnector" system="IN"/>
            </inbound>
            <outbound>
                <pass-through-router>
                    <vm:outbound-endpoint address="vm://out">
                        <transformers>
                            <encrypt-transformer strategy-ref="passwordEncryption"/>
                        </transformers>
                    </vm:outbound-endpoint>
                </pass-through-router>
            </outbound>
        </service>

        <service name="out">
            <inbound>
                <vm:inbound-endpoint address="vm://out">
                    <decrypt-transformer strategy-ref="passwordEncryption"/>
                </vm:inbound-endpoint>
            </inbound>
            <outbound>
                <pass-through-router>
                    <stdio:outbound-endpoint system="OUT"/>
                </pass-through-router>
            </outbound>
        </service>
    </model>

</mule>
