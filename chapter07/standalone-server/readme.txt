Mule Standalone Server Demo
---------------------------

Start this example with the script that is relevant for your OS.

After Mule is started, browse to http://localhost:8080/rig and refresh several times to see random integers.
