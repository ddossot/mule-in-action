Mule Standalone Server Demo
---------------------------

Start this example with the script that is relevant for your OS.

After Mule is started, browse to http://localhost:9756/rig and refresh several times to see random integers.
