Mule Chaining Router Example
---------------------------------

You must first build and install this project with: mvn clean install

Start this example with the script that is relevant for your OS.

Enter a destination when prompted.  To see the results comment out
the "chainingRouterReceiver" in chainging-router-config.xml
