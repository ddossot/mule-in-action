Mule FTP Transport Demos
---------------------------------

Modify conf/ftp-config.xml and replace the global-property values
with an FTP server to test against (consult the unit tests to
see how to use a Mock FTP server along with the MuleClient.) 

Start this example with the script that is relevant for your OS.

